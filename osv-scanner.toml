# OSV-Scanner vulnerability suppressions.
#
# Every entry must carry a `reason`. Entries for advisories that may eventually
# ship a fix carry an `ignoreUntil` date so the suppression expires and the
# advisory resurfaces for re-review; entries for permanent false positives omit
# it. List both the PyPI (PYSEC) and GitHub/CVE identifiers per advisory so the
# ignore matches whichever id OSV-Scanner surfaces.
#
# No suppressions are currently needed.
